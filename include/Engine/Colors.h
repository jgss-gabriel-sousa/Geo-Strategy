#pragma once
#include <SFML/Graphics/Color.hpp>

namespace Color{
    const sf::Color white = sf::Color(255,255,255,255);
    const sf::Color darkWhite = sf::Color(175,175,175,255);

    const sf::Color grey = sf::Color(141,141,141,255);
    const sf::Color lightGrey = sf::Color(198,198,198,255);

    const sf::Color lightBlack = sf::Color(34,34,34,255);
    const sf::Color black = sf::Color(0,0,0,255);

    const sf::Color green = sf::Color(0,179,44,255);
    const sf::Color lightGreen = sf::Color(0,255,0,255);
    const sf::Color darkGreen = sf::Color(0,78,56,255);

    const sf::Color red = sf::Color(255,0,0,255);

    const sf::Color blue = sf::Color(0,0,255,255);

    const sf::Color yellow = sf::Color(255,255,0,255);
}
